import React from "react";
import ReactDOM from "react-dom/client";

function MainPageComponent () {
    return(
        <React.Fragment>


        </React.Fragment>
    );
};

const root = ReactDOM.createRoot(document.getElementById('visually-hidden'));
root.render(<MainPageComponent/>);
